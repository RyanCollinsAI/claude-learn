---
description: Teach me one topic, from a measured edge, with a session note in the vault
argument-hint: <what to learn, e.g. the master theorem>
---

# /learn

Topic: **$ARGUMENTS**

Use the `learn` skill on this topic now - probe what is already known, map the route, then teach one step at a time. Read `SKILL.md` in that skill's own folder for the full procedure; do not improvise around it.

If `$ARGUMENTS` came through empty, ask in one line what to learn, and stop there until they answer.

`/learn review` and `/learn quiz me` mean the same thing: run the spaced-repetition ladder on whatever is due instead of starting a new topic.
